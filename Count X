def count_char_x(word,x):
  occur = 0
  for num in word:
    if num == x:
      occur += 1
  return occur
print(count_char_x("mississippi", "s"))
# should print 4
print(count_char_x("mississippi", "m"))
# should print 1
